import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    height: 64,
    backgroundColor: "#323238",
    borderRadius: 7,
    padding: 16,
    flexDirection: "row",
    alignItems: "center",
  },
  field: {
    flex: 1,
    color: "#FFFFFF",
    fontSize: 16,
  },
})
